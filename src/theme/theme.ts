import { Colours, Theme } from './types';

export const colours: Colours = {
  accept: 'green',
  decline: 'red',
  appBackground: '#e3e3e3',
  black: '#141127',
  white: '#ffffff',
};

const theme: Theme = {
  colours,
};

export default theme;
